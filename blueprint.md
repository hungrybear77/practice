# Lotto Number Generator

## Overview

This is a simple web application that generates random lottery numbers. Users can click a button to get a set of 6 unique numbers between 1 and 45.

## Features

*   Generate 6 unique random numbers.
*   Display the generated numbers to the user.
*   Modern and clean user interface.
*   Dark/light mode toggle with system preference fallback and local persistence.

## Plan

1.  **Add theme toggle UI**:
    *   Add a dark/light mode toggle button in the header toolbar.
2.  **Implement theme system**:
    *   Use CSS variables for theme colors and add dark overrides.
    *   Persist theme selection in local storage and default to system preference.
